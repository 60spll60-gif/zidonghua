# 🌐 知识库网站部署指南

> 目标：把知识库网页发布成网址，手机、电脑随时打开就能看。

---

## 部署方式：CloudStudio 沙箱（WorkBuddy 内置，0 元）

### 当前链接

```
https://19d67d09746b48248262f74ca8548ec7.bj9.agentos-app.net/index.html
```

> ⚠️ **iPhone 用户注意**：链接必须带 `/index.html` 后缀，否则 iOS Safari 会触发下载！

### 使用方法

对我说以下任意一句话：

- 「部署到 CloudStudio」
- 「更新网站」
- 「重新部署」

我会自动把 `部署站点/` 目录上传并生成新的分享链接。

### 更新内容

修改 `部署站点/` 里的文件后，再对我说「部署到 CloudStudio」即可。

### 手机使用

1. iPhone Safari 打开 `.../index.html`（带后缀）
2. 底部「分享」→「添加到主屏幕」
3. 以后从主屏图标打开 = 像 App 一样

---

## 🌍 自定义域名：formosus.me ✅ 已生效（2026-09-03）

> 正式访问地址：**https://formosus.me**（Cloudflare 托管 + 免费 HTTPS）

### 当前配置状态

| 项目 | 状态 |
|------|------|
| DNS 托管 | ✅ Cloudflare（hope / rex.ns.cloudflare.com） |
| DNS 记录 | ✅ 自动创建（Cloudflare 代理橙云） |
| HTTPS 证书 | ✅ *.formosus.me 通配符证书，有效期至 2026-12-02 |
| 托管平台 | ✅ Cloudflare Workers（静态资源，项目名「圆诗-0cf8」） |
| 验收测试 | ✅ 主页/登录页/打卡页全部 200 |

### 可用地址

- **https://formosus.me** —— 主入口（自动跳转登录页）
- https://formosus.me/checkin.html —— 每日打卡
- https://handsome.boy.ll.formosus.me —— 彩蛋子域名（同样可访问）

### 更新网站的方法

修改 `部署站点/` 里文件后，两种方式任选：
1. Cloudflare 控制台 → Workers 和 Pages → 圆诗-0cf8 → **创建新版本/上传** 直接替换
2. 对 WorkBuddy 说「部署到 CloudStudio」（旧链路，双通道备用）

## 🤖 GitHub Actions 自动部署

仓库：`https://github.com/60spll60-gif/zidonghua`

以后本地知识库更新后，运行：

```powershell
python _系统/03-脚本/自动迭代并推送.py
```

脚本会依次执行知识汇出、周回顾、统计、dashboard 重建和部署包校验，然后提交并推送到 GitHub。`.github/workflows/deploy.yml` 会在 push、每周五 18:00（北京时间）或手动触发时执行 Cloudflare 部署，并检查线上 Linux 内容。

### GitHub 仓库需要配置的 Secrets

> 名称必须完全匹配，尤其是 `CLOUDFLARE_ACCOUNT_ID`，不能写成 `CLOUDFLARE_SCOUNT_ID` 或其他名称。

在仓库 **Settings → Secrets and variables → Actions** 中添加：

| 名称 | 用途 |
|---|---|
| `CLOUDFLARE_API_TOKEN` | Cloudflare API Token，不要写进代码 |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare Account ID |

### GitHub 仓库需要配置的 Variables

当前线上是 Cloudflare Workers 静态资源，建议设置：

| 名称 | 值 |
|---|---|
| `CLOUDFLARE_DEPLOY_MODE` | `workers` |
| `CLOUDFLARE_WORKER_NAME` | Cloudflare 控制台中的真实 Worker 名称，只能包含英文、数字和短横线，例如 `yuan-shi-0cf8`；不要填写中文显示别名 |
| `SITE_URL` | `https://formosus.me` |
| `WRANGLER_VERSION` | 可选，默认 `4.129.0`，用于锁定部署工具版本 |

如果实际项目是 Cloudflare Pages，将部署模式改为 `pages`，并配置 `CLOUDFLARE_PROJECT_NAME`。

部署工作流会先执行 `wrangler whoami` 验证 Token，再执行发布和线上 Linux 内容检查。

---

## 部署包结构

```
部署站点/
├── login.html           # 登录页（手机号验证码 + 微信扫码，前端模拟）
├── index.html           # 知识库仪表盘（马卡龙设计系统 + 本地 Chart.js/marked）
├── checkin.html         # 每日打卡页（计划模板 + 目标 + 周视图 + 成就徽章）
├── libs/                # 本地化 JS 库（chart.umd.js / marked.min.js）
├── manifest.json        # PWA 配置（添加到主屏幕）
├── sw.js                # Service Worker（离线缓存 v3，含 login.html）
└── icons/              # 9 个 favicon/app 图标
```

---

## 功能说明（2026-09 更新）

- **用户登录**：打开网站先进入 `login.html`，支持手机号验证码登录（验证码为前端模拟，显示在页面底部提示条）和微信扫码登录（模拟流程）。真实短信/微信 OAuth 需后端支持。
- **登录态跨页同步**：登录后跳转仪表盘/打卡页自动携带登录态；退出登录会清除所有页面的记录。
- **每日打卡增强**：5 套内置计划模板一键套用（含 Linux 学习日）、每日目标档位（60/80/100%）、本周打卡视图、30 天热力图、9 枚成就徽章、打卡海报文案复制。
- **Linux 学习支柱**：独立 MOC、基础知识、练习题和每周迭代任务；继续纳入知识汇出、周回顾、统计与 dashboard 自动刷新。
- **旧数据兼容**：升级后首次打开会自动继承旧版打卡数据（按 80% 目标档补算达成标记）。

---

*最后更新：2026-09-03*
